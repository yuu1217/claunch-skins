[project]
name = "claunch-skins"
version = "0.1.0"
description = "フラットなダークカラーの CLaunch スキン生成ツール"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pillow>=12.2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/flat_dark"]
